@inject NavigationManager navigation

@code {
    protected override void OnInitialized()
    {
        navigation.NavigateToLogin($"/login?ReturnUrl={Uri.EscapeDataString(navigation.Uri)}");
    }
}
